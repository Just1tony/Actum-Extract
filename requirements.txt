# ==============================================================================
# ActumAI Extract — Production dependency manifest
# Pinned to minor-version ranges to keep the build reproducible while still
# allowing patch-level security fixes.
# ==============================================================================

# --- Web framework / ASGI server ---
fastapi==0.115.6
uvicorn[standard]==0.32.1
python-multipart==0.0.20        # required by FastAPI for UploadFile / form parsing

# --- Data validation & settings ---
pydantic>=2.7,<3.0.0
pydantic-settings==2.7.0

# --- LLM structured extraction stack ---
instructor==1.7.2
openai==1.58.1

# --- Document parsing ---
pypdf==5.1.0

# --- Resilience ---
tenacity==9.0.0

# --- Testing ---
pytest==8.3.4
pytest-asyncio==0.25.0
httpx==0.28.1
respx==0.22.0

# --- Observability helpers ---
python-json-logger==2.0.7
